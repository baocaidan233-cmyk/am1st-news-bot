notion:
  source_db_id: ""       # env: NOTION_SOURCE_DB_ID — RSS feed table (url + cookie + in_use)
  candidate_db_id: ""    # env: NOTION_CANDIDATE_DB_ID — "AM1ST-n8n-Channel" candidate pool, shared by ingestion + publish cycles
  hot_topics_db_id: ""    # env: NOTION_HOT_TOPICS_DB_ID — shared "AM1ST热点标记" table, manual breaking-news flag, see core/hot_topics.py
  alert_user_id: ""       # env: NOTION_ALERT_USER_ID — @mentioned on cookie/extraction failures
  # Source props confirmed against the live "rss_n8n_am1st_news" database 2026-08-03.
  # Candidate props confirmed against the original n8n export's node field lists 2026-08-04 — not yet verified against a live schema read, check before first run.
  source_props:
    in_use: "in_use"
    feed_url: "RSS"
    name: "Name"
    cookie: "cookie"
    domain: "website"
  candidate_props:
    title: "title"
    url: "url"
    author: "author"
    description: "description"
    published_at: "published_at"
    post_content: "post_content"
    llm_score: "llm_score"
    llm_comment: "llm_comment"
    content: "content"
    url_hash: "url_hash"
    send_status: "send_status"
    heat_score: "heat_score"
    event_first_seen_at: "event_first_seen_at"
    is_hot: "is_hot"
  hot_topics_props:
    name: "Name"
    channel: "Channel"
    active: "In_Use"

redis:
  key_prefix: "am1st:url_hash:"
  ttl_seconds: 864000  # 10 days
  caption_prefix: "am1st:caption:"
  caption_ttl_seconds: 172800  # 48h — see core/config.py RedisConfig comment

openai:
  chat_model: "gpt-4o-mini"  # every OpenAI-backed call — Writer, Scorer, PriorityRanker, EventVerifier — see core/config.py comment
  embedding_model: "text-embedding-3-small"
  scoring_prompt_file: "prompts/scoring_prompt.txt"
  content_gen_prompt_file: "prompts/content_gen_prompt.txt"
  score_threshold: 5.0  # inclusive — score >= 5.0 passes

dedup:
  # 2026-09-01: raised 0.8 -> 0.85, live A/B test at Caidan's request. Real
  # data from logs/event_identity_decisions.jsonl (818 lines, pulled and
  # analyzed 2026-09-01) showed the 0.80-0.90 cosine band was NOT a safe
  # "definite duplicate" zone: 21/113 (~19%) of real pairs scoring there
  # were actually RELATED_DIFFERENT_EVENT, not the same occurrence — most
  # exposed on Layer 2 intra-batch clustering (main.py), which merges on
  # cosine alone with no entity/LLM check. Baseline snapshot before this
  # change: 1677 distinct event_ids / 2211 points in am1st_events.
  #
  # 2026-09-01, later same day: reverted back to 0.8 — the 0.85 change
  # above was justified by analyzing event_identity_decisions.jsonl
  # entries logged by EventVerifier.same_event(), but that same live test
  # run separately confirmed same_event()/related_event() were making real
  # judgment errors in both directions while running on gpt-5-nano (see
  # agents/scorer.py's docstring, core/event_identity.py's EventVerifier
  # docstring) — every log entry from that window was labeled by the same
  # unreliable judge, so the "~19% of 0.80-0.90 pairs were actually
  # different events" analysis behind the 0.85 change can't be trusted
  # either. Per the user's explicit call: don't act on data produced
  # during the nano_model window until it's been reproduced with the
  # reverted gpt-4o-mini judge.
  semantic_threshold: 0.85  # cosine similarity, both intra-batch and cross-cycle layers -- raised from 0.8 2026-09-16, a week of gpt-4o-mini event_identity_decisions.jsonl data showed the 0.8-0.9 band still had a 13.6% wrong-merge rate (127/931)

  # 跨周期查重灰度区下限，2026-09-18 从 heat.related_threshold(0.6) 独立出来并提到 0.7。
  # 依据：用独立 LLM（刻意不用项目自己的 same_event_prompt，那是被审计对象）复核
  # logs/event_identity_decisions.jsonl 里 400 对真实灰度配对，发现跨周期层
  # **73% 的"判重复"是误判**，且集中在低余弦端：
  #     0.60-0.65 误判 90% | 0.65-0.70 82% | 0.70-0.75 59% | 0.75-0.80 39% | 0.80-0.85 23%
  # 0.60-0.70 根本不是灰度区：283 对样本里真同一事件只有 10%，且该区间连 LLM
  # 复核都误判 71%（实体规则 89%）——余弦 0.6-0.7 本身不携带足够信息，换任何
  # 判官都救不回来。切在 0.7 的收益比是「救回约 169 条误杀 : 放过约 26 条真重复」= 6.5:1，
  # 也符合既定编辑原则：同一批事实的不同角度不算重复。
  # 不影响 heat.related_threshold、批内 joined_related 归簇、EventStore.peek*，那些保持 0.6。
  gray_zone_floor: 0.7

entity_verifier:
  hub_event_count_threshold: 2   # shared token only counts as evidence if it's been the core of fewer than this many past events
  pair_cooccur_max: 1            # two hub tokens together still count if this exact pair has co-occurred in at most this many past events
  min_doc_freq_for_core: 2       # a token needs >=this many of an event's own articles to join its persisted core_entities
  hub_key_prefix: "am1st:hub:"
  same_event_prompt_file: "prompts/same_event_prompt.txt"
  update_subtype_prompt_file: "prompts/update_subtype_prompt.txt"
  related_event_prompt_file: "prompts/related_event_prompt.txt"
  log_path: "logs/event_identity_decisions.jsonl"
  top_k: 5  # verify up to this many historical event candidates, not just the top-1 cosine match — see core/config.py
  subtype_restatement_weight: 0.2
  subtype_corroboration_weight: 1.0
  subtype_core_update_weight: 1.5

hot_topics:
  channel_name: "AM1ST"      # this bot's own tag in the shared Notion table's Channel multi-select column
  match_threshold: 0.5       # cosine floor for "this candidate is about a currently-flagged hot topic" — calibrated 2026-08-31 via live embedding tests (see core/config.py), still not independently validated against real am1st_events data
  ttl_hours: 24               # both the Notion flag's own freshness window and how long a matched event stays "hot"
  fast_poll_seconds: 180      # main_publish.py's short-poll interval while an unpublished is_hot candidate exists

heat:
  related_threshold: 0.6      # cosine floor for "same event, different source" — below dedup.semantic_threshold
  window_hours: 240           # 10 days — matches redis.ttl_seconds' convention, wider than qdrant.cross_cycle_window_hours (72h) so multi-day events don't fragment
  major_outlets:
    - "Reuters"
    - "Financial Times"
    - "Wall Street Journal"
    - "CNN"
    - "CBS"
    - "Fox News"
    - "New York Post"
    - "Just The News"
  major_outlet_weight: 2.0    # vs 1.0 for any other corroborating source

qdrant:
  collection: "am1st_embeddings"           # ingestion-side cross-cycle dedup cache (title+description)
  posted_collection: "am1st_posting_news_embedding"  # publish-side "already posted" cache (post_content)
  events_collection: "am1st_events"        # event aggregation — groups of points per underlying event, see HeatConfig/EventStore
  cross_cycle_window_hours: 240  # widened 2026-09-13, matches heat.window_hours and publish.posted_dedup_window_hours
  cleanup_retention_days: 10  # for the separate, not-yet-built daily cleanup job
  timeout_seconds: 15  # AsyncQdrantClient has no timeout by default — real hang observed 2026-08-11

extraction:
  timeout_seconds: 20
  min_text_length: 200

gettr:
  api_url: "https://gettr.com/api/u/post"

publish:
  interval_seconds: 1800       # 30 minutes
  candidate_min_score: 5.0     # Notion query floor (lower of weekday/weekend below) — unchanged, still the lower of the two
  weekday_min_score: 6.0        # raised to 7.0 2026-09-14 per 7-day audit, reverted same day — user judged too few real 7+ stories on a given day, tier would starve too often before falling back
  weekend_min_score: 5.0
  candidate_max_age_hours: 24  # Notion query ceiling (wider of weekday/weekend below)
  weekday_max_age_hours: 12    # weekdays: keep the "same-day news" freshness rule
  weekend_max_age_hours: 24    # weekends: real volume is lower — widened 2026-09-05 per user request
  fresh_hours: 4
  batch_min: 3
  batch_max: 10  # reverted 2026-09-02, same day — lowered to 5 earlier, but the very first cycle under 5 hit "all candidates were duplicates, nothing to publish" on a day with heavy repeat coverage of a few ongoing stories (Iran, socialism vote); 5->10 wasn't saving much real token cost anyway (per-call cost is small), not worth the risk of skipped cycles. See core/config.py's PublishConfig.batch_max comment.
  priority_rank_prompt_file: "prompts/priority_rank_prompt.txt"
  posted_dedup_window_hours: 240  # was 24h — widened 2026-08-07, matches heat.window_hours (10 days)
  posted_dedup_threshold: 0.70  # stricter than ingestion's 0.8 — deliberate, fully-autonomous posting errs toward under-posting
  max_widen_attempts: 3        # widen-on-empty: try up to this many batch_max-sized chunks of the eligible pool before giving up this cycle
  # 深夜质量闸门 2026-09-17（美东时区，与 weekday_min_score 同一日历基准）。
  # 实测依据：745 个发布批次（09-04..09-17），美东 0-6 点产出 28% 的发布周期，
  # 但赢家 llm>=7 的只有 10%（白天各时段是 29-47%），74% 的深夜批次里连一条
  # "新鲜且高分"的稿都没有。这些深夜帖在控制帖龄后，实际互动比晚间低约 36%。
  # 2026-09-18 定为 6.0 —— 即分数闸门实际关闭，深夜只靠下面的节奏下限降量。
  # 依据：7 天 Notion 候选数据显示深夜(ET 0-6)入库 277 条的分数是
  # 5.0x140 / 6.0x126 / 7.0x9 / 8.0x2，7 分以上平均每晚仅 1.6 条，而每晚约有
  # 9 个发布周期。7.0 门槛在第一个完整夜产出 0 帖（连续 7 个周期 select_batch
  # 全空），正是用户否掉的"深夜停发"。时效不是瓶颈：深夜入库的文章距其发布
  # 中位只有 0.1 小时，所以 4h/6h/12h/24h 各种时效变体都是同样的 137 条。
  # 分数又基本是整数，6.0 和 7.0 之间没有可用档位。深夜质量差是真的，但根源
  # 在上游——深夜时段来源本身就不产 7 分级内容——不是这一层能解决的。
  #
  # 以下为早先记录，保留是因为它解释了 8.0 -> 7.0 那一步：
  # （65% 的深夜批次有 8.0+ 候选），但 8.0 分候选在 09-16 从每日约 11% 掉到
  # 2%、09-17 归零。09-17 06:22 ET 实查候选池：289 条里 175 条 5.0、105 条 6.0、
  # 9 条 7.0、8.0 一条没有 —— 8.0 的门槛等同于深夜停发，而那不是需求。
  # 7.0 仍能把深夜帖整体抬出 6.0 档（改动前只有 10% 的深夜赢家到 7 分），
  # 同时是可满足的；降量交给下面的节奏下限做。
  # 若 8.0 供给恢复可再调高。该塌陷与 09-16 把 dedup.semantic_threshold
  # 0.8->0.85 时间吻合（阈值提高→事件更碎→heat_score 走低→打分 prompt 里
  # 靠 heat 触发的 8 档失效），但 1.5 天数据不足以定因果，且 heat 自 09-04
  # 起本就在下滑。此事单独跟踪，不该由这个闸门来补偿。
  # night_start_hour == night_end_hour 即关闭该窗口。
  night_start_hour: 0
  night_end_hour: 7
  night_min_score: 6.0
  staleness_check_hours_floor: 72  # only run the staleness LLM call when event_first_seen_at is at least this old — reuses dedup.cross_cycle_window_hours' 72h convention

dynamic_publish:
  min_interval_seconds: 900    # 15 min floor
  max_interval_seconds: 2340   # 39 min ceiling -- user's explicit cap (2026-09-06)
  calibration_log_path: 'logs/dynamic_interval_calibration.jsonl'
  calibration_window_days: 14.0
  calibration_min_samples: 50   # below this many recent samples, use the *_default bands below instead
  trending_check_top_k: 25
  backlog_low_default: 145.0    # real p10, eligible-candidate count, 2026-09-06..09-12
  backlog_high_default: 382.0   # real p90, same sample
  heat_low_default: 0.6         # raw heat_score (compared in ln(1+x) space internally)
  heat_high_default: 25.0
  trending_low_default: 0.35    # cosine similarity vs Google News trending headlines
  trending_high_default: 0.65   # matches agents/priority_ranker.py's own _TRENDING_SIM_HIGH

  # 深夜节奏下限 2026-09-17，与 publish.night_min_score 是同一个改动的两半。
  # 在 compute_dynamic_interval() 的 [min, max] 夹取之后才施加，因此会故意
  # 突破 max_interval_seconds——39 分钟那个上限是白天的响应速度约束，不代表
  # 深夜也该那么快。45 分钟 × 7 小时窗口 ≈ 9 个周期，乘以 65% 的够格率，
  # 落在 5-7 帖/夜（现状 16.5）。
  night_min_interval_seconds: 2700


max_publish_age_hours: 3
poll_interval_seconds: 600  # 10 minutes, matches the original n8n cadence
cycle_timeout_seconds: 540  # 9 min — real n8n cycles ran ~5min, almost never past 7min; hard-cuts a stuck cycle so the next one always starts on schedule
alert_cooldown_seconds: 21600  # 6h — don't re-@mention for a still-broken source every cycle

# 2026-09-22 — 预览图坏掉时改发我方渲染的标题卡，见 core/config.py 的 PosterConfig
poster:
  enabled: true
